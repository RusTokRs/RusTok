[advisories]
# Lockfile-only optional dependency paths. Each entry is documented and expires
# on 2026-09-13; do not extend an ignore without fresh reachability evidence.
ignore = [
  "RUSTSEC-2023-0071", # optional SQLx MySQL path; absent from locked all-feature/all-target graph
  "RUSTSEC-2026-0235", # optional rust_decimal rkyv path; absent from locked all-feature/all-target graph
]
