name: One-off Wire API Compatibility Master Runner

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  wire-master-runner:
    if: >-
      github.event.issue.title == '[automation] wire API compatibility master runner' &&
      (github.event_name == 'issues' || github.event.comment.body == '/wire-api-compatibility-master')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - run: git pull --rebase origin main
      - name: Apply exact master-runner patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path("scripts/verify/verify-all.sh")
          source = path.read_text()

          def replace_once(old: str, new: str) -> None:
              global source
              count = source.count(old)
              if count != 1:
                  raise SystemExit(f"expected one verify-all match, found {count}: {old!r}")
              source = source.replace(old, new, 1)

          replace_once(
              '    echo "  api-surface-contract  Check manifest-driven GraphQL/REST composition without compiling"\n',
              '    echo "  api-surface-contract  Check manifest-driven GraphQL/REST composition without compiling"\n'
              '    echo "  api-compatibility-self-test  Verify OpenAPI/GraphQL semantic comparator fixtures"\n'
              '    echo "  api-compatibility-contract  Verify API exporter, diff workflow and exception governance"\n',
          )
          replace_once(
              '    "verify-api-surface-contract.mjs:API Surface Contract"\n',
              '    "verify-api-surface-contract.mjs:API Surface Contract"\n'
              '    "verify-api-compatibility-self-test.mjs:API Compatibility Comparator Fixtures"\n'
              '    "verify-api-compatibility-contract.mjs:API Compatibility Gate Structure"\n',
          )

          path.write_text(source)
          workflow = Path(".github/workflows/one-off-wire-api-compatibility-master.yml")
          if not workflow.exists():
              raise SystemExit("one-off workflow is missing")
          workflow.unlink()
          PY
      - name: Verify patch shape
        run: |
          git diff --check
          grep -Fq 'verify-api-compatibility-self-test.mjs:API Compatibility Comparator Fixtures' scripts/verify/verify-all.sh
          grep -Fq 'verify-api-compatibility-contract.mjs:API Compatibility Gate Structure' scripts/verify/verify-all.sh
          test ! -e .github/workflows/one-off-wire-api-compatibility-master.yml
      - name: Commit master-runner wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test(api): wire compatibility verification suite"
          git push origin HEAD:main
      - name: Close trigger issue
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh issue close "${{ github.event.issue.number }}" --comment "API compatibility fixture and structural gates wired to verify-all; one-off workflow removed itself."
