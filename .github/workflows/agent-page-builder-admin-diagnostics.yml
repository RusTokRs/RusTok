name: Agent Page Builder Admin Diagnostics

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    if: ${{ github.event.pull_request.head.ref == 'agent/ci-baseline-page-builder-diagnostics' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.93.1
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: agent-page-builder-admin-diagnostics
      - name: Capture formatter and Page Builder admin tests
        shell: bash
        run: |
          mkdir -p /tmp/page-builder-admin
          set +e
          cargo fmt --all -- --check > /tmp/page-builder-admin/fmt.log 2>&1
          echo $? > /tmp/page-builder-admin/fmt.status
          cargo test -p rustok-page-builder-admin --lib -- --nocapture \
            > /tmp/page-builder-admin/test.log 2>&1
          echo $? > /tmp/page-builder-admin/test.status
          exit 0
      - uses: actions/upload-artifact@v7
        with:
          name: agent-page-builder-admin-diagnostics
          path: /tmp/page-builder-admin
      - name: Fail on diagnostics
        shell: bash
        run: |
          test "$(cat /tmp/page-builder-admin/fmt.status)" = 0
          test "$(cat /tmp/page-builder-admin/test.status)" = 0
