name: One-off Retire Dependency Waivers

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/one-off-dependency-waiver-diagnostic.yml

permissions:
  contents: write
  issues: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  SECURITY_ISSUE: 1851

jobs:
  retire:
    if: >-
      github.event.head_commit.message == 'test(security): trace waiver dependency roots'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: 22
      - name: Install exact Rust toolchain
        run: |
          rustup toolchain install 1.96.0 --profile minimal --no-self-update
          rustup default 1.96.0
      - name: Verify feature policy and all-target inverse trees
        shell: bash
        run: |
          set -euo pipefail
          node scripts/verify/verify-dependency-feature-hygiene.mjs
          test -z "$(cargo tree --locked -i atomic-polyfill --workspace --all-features --target all 2>/dev/null)"
          test -z "$(cargo tree --locked -i rsa --workspace --all-features --target all 2>/dev/null)"
      - name: Trace workspace-root metadata paths
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          cargo metadata --locked --all-features --format-version 1 \
            > /tmp/cargo-metadata-all-features.json
          set +e
          python3 scripts/maintenance/retire-dependency-waivers.py \
            > /tmp/waiver-prune.log 2>&1
          status=$?
          set -e
          {
            printf 'Trace the metadata paths that keep the temporary-waiver packages reachable.\n\n'
            printf '## Workspace-root dependency paths\n\n```text\n'
            cat /tmp/waiver-prune.log
            printf '\n```\n'
          } > /tmp/waiver-prune-failure.md
          gh issue edit "$SECURITY_ISSUE" \
            --repo "$GITHUB_REPOSITORY" \
            --body-file /tmp/waiver-prune-failure.md
          exit "$status"
