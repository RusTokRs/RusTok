name: Agent Forum group-state GraphQL 20AN

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/forum-notification-group-state-graphql-20an'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Apply FORUM-20AN source slice
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import json

          ROOT = Path('.')

          def read(path):
              return (ROOT / path).read_text()

          def write(path, content):
              target = ROOT / path
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_text(content)

          def replace_once(path, old, new):
              source = read(path)
              count = source.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one anchor, found {count}: {old[:120]!r}')
              write(path, source.replace(old, new, 1))

          graphql = 'crates/rustok-notifications/src/graphql.rs'
          replace_once(
              graphql,
              '''use crate::{
    DEFAULT_NOTIFICATION_INBOX_PAGE_SIZE, NotificationError, NotificationInboxGroupSummaryPage,
    NotificationInboxItem, NotificationInboxStorefrontGroupItemsRequest,
    NotificationInboxStorefrontGroupSummaryRequest, NotificationInboxStorefrontOpenDecision,
    NotificationInboxStorefrontOpenRequest, NotificationInboxStorefrontPort,
    NotificationInboxUnreadCountRequest, NotificationInboxUnreadCountService,
    in_process_notification_inbox_storefront_port,
};''',
              '''use crate::{
    DEFAULT_NOTIFICATION_INBOX_PAGE_SIZE, NotificationError, NotificationInboxGroupStateAction,
    NotificationInboxGroupStatePage, NotificationInboxGroupSummaryPage, NotificationInboxItem,
    NotificationInboxStorefrontGroupItemsRequest, NotificationInboxStorefrontGroupStateRequest,
    NotificationInboxStorefrontGroupSummaryRequest, NotificationInboxStorefrontOpenDecision,
    NotificationInboxStorefrontOpenRequest, NotificationInboxStorefrontPort,
    NotificationInboxUnreadCountRequest, NotificationInboxUnreadCountService,
    in_process_notification_inbox_storefront_port,
};''',
          )
          replace_once(
              graphql,
              '''const GRAPHQL_READ_DEADLINE: Duration = Duration::from_secs(5);
const MAX_NOTIFICATION_ID_BYTES: usize = 64;

#[derive(Default)]
pub struct NotificationsQuery;''',
              '''const GRAPHQL_READ_DEADLINE: Duration = Duration::from_secs(5);
const GRAPHQL_WRITE_DEADLINE: Duration = Duration::from_secs(5);
const MAX_NOTIFICATION_ID_BYTES: usize = 64;
const MAX_IDEMPOTENCY_KEY_BYTES: usize = 128;

#[derive(Default)]
pub struct NotificationsQuery;

#[derive(Default)]
pub struct NotificationsMutation;''',
          )
          replace_once(
              graphql,
              '''pub enum GqlNotificationInboxOpenDecision {
    Allowed,
    Unavailable,
}

#[derive(Clone, Debug, Eq, PartialEq, SimpleObject)]
pub struct GqlNotificationTemplateField {''',
              '''pub enum GqlNotificationInboxOpenDecision {
    Allowed,
    Unavailable,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq, Enum)]
#[graphql(name = "NotificationInboxGroupStateAction")]
pub enum GqlNotificationInboxGroupStateAction {
    MarkRead,
    MarkUnread,
    Archive,
}

#[derive(Clone, Debug, Eq, PartialEq, SimpleObject)]
pub struct GqlNotificationTemplateField {''',
          )
          replace_once(
              graphql,
              '''pub struct GqlNotificationInboxOpenAuthorization {
    pub decision: GqlNotificationInboxOpenDecision,
    pub route: Option<String>,
}

#[Object]
impl NotificationsQuery {''',
              '''pub struct GqlNotificationInboxOpenAuthorization {
    pub decision: GqlNotificationInboxOpenDecision,
    pub route: Option<String>,
}

#[derive(Clone, Debug, Eq, PartialEq, SimpleObject)]
pub struct GqlNotificationInboxGroupStatePage {
    pub scanned: u64,
    pub changed: u64,
    pub next_cursor: Option<String>,
    pub has_more: bool,
}

#[Object]
impl NotificationsQuery {''',
          )
          replace_once(
              graphql,
              '''}

#[derive(Clone)]
struct AuthenticatedInboxScope {''',
              '''}

#[Object]
impl NotificationsMutation {
    async fn notification_inbox_apply_group_state(
        &self,
        ctx: &Context<'_>,
        group_key: String,
        action: GqlNotificationInboxGroupStateAction,
        cursor: Option<String>,
        limit: Option<i32>,
        idempotency_key: String,
    ) -> Result<GqlNotificationInboxGroupStatePage> {
        let scope = authenticated_scope(ctx)?;
        require_module_enabled(ctx, MODULE_SLUG).await?;
        let idempotency_key = parse_idempotency_key(idempotency_key)?;
        let limit = parse_limit(limit)?;
        let page = grouped_storefront_port(ctx)?
            .apply_group_state(
                scope.write_port_context("group-state", idempotency_key),
                NotificationInboxStorefrontGroupStateRequest {
                    group_key,
                    action: map_group_action_to_owner(action),
                    cursor,
                    limit,
                },
            )
            .await
            .map_err(map_port_error)?;
        Ok(map_group_state_page(page))
    }
}

#[derive(Clone)]
struct AuthenticatedInboxScope {''',
          )
          replace_once(
              graphql,
              '''impl AuthenticatedInboxScope {
    fn port_context(&self, operation: &'static str) -> PortContext {
        let mut context = PortContext::new(
            self.tenant_id.to_string(),
            self.actor.clone(),
            self.locale.clone(),
            format!("notifications-graphql-{operation}-{}", Uuid::new_v4()),
        )
        .with_deadline(GRAPHQL_READ_DEADLINE)
        .with_channel("storefront");
        for claim in &self.claims {
            context = context.with_claim(claim.clone());
        }
        context
    }
}''',
              '''impl AuthenticatedInboxScope {
    fn port_context(&self, operation: &'static str) -> PortContext {
        self.base_port_context(operation, GRAPHQL_READ_DEADLINE)
    }

    fn write_port_context(&self, operation: &'static str, idempotency_key: String) -> PortContext {
        self.base_port_context(operation, GRAPHQL_WRITE_DEADLINE)
            .with_idempotency_key(idempotency_key)
    }

    fn base_port_context(&self, operation: &'static str, deadline: Duration) -> PortContext {
        let mut context = PortContext::new(
            self.tenant_id.to_string(),
            self.actor.clone(),
            self.locale.clone(),
            format!("notifications-graphql-{operation}-{}", Uuid::new_v4()),
        )
        .with_deadline(deadline)
        .with_channel("storefront");
        for claim in &self.claims {
            context = context.with_claim(claim.clone());
        }
        context
    }
}''',
          )
          replace_once(
              graphql,
              '''fn invalid_notification_id() -> async_graphql::Error {
    public_error(
        "NOTIFICATION_VALIDATION_ERROR",
        "notification id is invalid",
        false,
    )
}

fn map_group_summary_page(''',
              '''fn invalid_notification_id() -> async_graphql::Error {
    public_error(
        "NOTIFICATION_VALIDATION_ERROR",
        "notification id is invalid",
        false,
    )
}

fn parse_idempotency_key(value: String) -> Result<String> {
    let value = value.trim();
    if value.is_empty()
        || value.len() > MAX_IDEMPOTENCY_KEY_BYTES
        || value.chars().any(char::is_control)
    {
        return Err(public_error(
            "NOTIFICATION_VALIDATION_ERROR",
            "notification idempotency key is invalid",
            false,
        ));
    }
    Ok(value.to_string())
}

fn map_group_summary_page(''',
          )
          replace_once(
              graphql,
              '''fn map_open_decision(
    decision: NotificationInboxStorefrontOpenDecision,
) -> GqlNotificationInboxOpenAuthorization {''',
              '''fn map_group_state_page(
    page: NotificationInboxGroupStatePage,
) -> GqlNotificationInboxGroupStatePage {
    GqlNotificationInboxGroupStatePage {
        scanned: u64::from(page.scanned),
        changed: u64::from(page.changed),
        next_cursor: page.next_cursor,
        has_more: page.has_more,
    }
}

fn map_group_action_to_owner(
    action: GqlNotificationInboxGroupStateAction,
) -> NotificationInboxGroupStateAction {
    match action {
        GqlNotificationInboxGroupStateAction::MarkRead => {
            NotificationInboxGroupStateAction::MarkRead
        }
        GqlNotificationInboxGroupStateAction::MarkUnread => {
            NotificationInboxGroupStateAction::MarkUnread
        }
        GqlNotificationInboxGroupStateAction::Archive => {
            NotificationInboxGroupStateAction::Archive
        }
    }
}

fn map_open_decision(
    decision: NotificationInboxStorefrontOpenDecision,
) -> GqlNotificationInboxOpenAuthorization {''',
          )

          replace_once(
              'crates/rustok-notifications/src/lib.rs',
              '''#[cfg(feature = "server")]
pub use graphql::{GqlNotificationInboxUnreadCount, NotificationsQuery};''',
              '''#[cfg(feature = "server")]
pub use graphql::{
    GqlNotificationInboxGroupStateAction, GqlNotificationInboxGroupStatePage,
    GqlNotificationInboxUnreadCount, NotificationsMutation, NotificationsQuery,
};''',
          )
          replace_once(
              'crates/rustok-notifications/rustok-module.toml',
              '''[provides.graphql]
query = "graphql::NotificationsQuery"
runtime_data_factory = "graphql::attach_schema_data"''',
              '''[provides.graphql]
query = "graphql::NotificationsQuery"
mutation = "graphql::NotificationsMutation"
runtime_data_factory = "graphql::attach_schema_data"''',
          )

          adapter = 'crates/rustok-notifications/storefront/src/transport/graphql_adapter.rs'
          replace_once(
              adapter,
              '''    NotificationStorefrontGroupSummary, NotificationStorefrontGroupSummaryPage,
    NotificationStorefrontGroupSummaryRequest, NotificationStorefrontItem,
    NotificationStorefrontItemState, NotificationStorefrontOpenDecision,
    NotificationStorefrontOpenRequest, NotificationStorefrontPriority,
    NotificationStorefrontUnreadCount,
};''',
              '''    NotificationStorefrontGroupStateAction, NotificationStorefrontGroupStateCommand,
    NotificationStorefrontGroupStatePage, NotificationStorefrontGroupSummary,
    NotificationStorefrontGroupSummaryPage, NotificationStorefrontGroupSummaryRequest,
    NotificationStorefrontItem, NotificationStorefrontItemState, NotificationStorefrontOpenDecision,
    NotificationStorefrontOpenRequest, NotificationStorefrontPriority,
    NotificationStorefrontUnreadCount,
};''',
          )
          replace_once(
              adapter,
              '''const OPEN_AUTHORIZATION_QUERY: &str = r#"
query NotificationStorefrontAuthorizeOpen($notificationId: String!) {
  notificationInboxAuthorizeOpen(notificationId: $notificationId) {
    decision
    route
  }
}
"#;''',
              '''const OPEN_AUTHORIZATION_QUERY: &str = r#"
query NotificationStorefrontAuthorizeOpen($notificationId: String!) {
  notificationInboxAuthorizeOpen(notificationId: $notificationId) {
    decision
    route
  }
}
"#;
const GROUP_STATE_MUTATION: &str = r#"
mutation NotificationStorefrontApplyGroupState(
  $groupKey: String!
  $action: NotificationInboxGroupStateAction!
  $cursor: String
  $limit: Int
  $idempotencyKey: String!
) {
  notificationInboxApplyGroupState(
    groupKey: $groupKey
    action: $action
    cursor: $cursor
    limit: $limit
    idempotencyKey: $idempotencyKey
  ) {
    scanned
    changed
    nextCursor
    hasMore
  }
}
"#;''',
          )
          replace_once(
              adapter,
              '''struct OpenAuthorizationVariables {
    #[serde(rename = "notificationId")]
    notification_id: String,
}

#[derive(Clone, Copy, Debug, Serialize)]
#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
enum GroupItemStateWire {''',
              '''struct OpenAuthorizationVariables {
    #[serde(rename = "notificationId")]
    notification_id: String,
}

#[derive(Debug, Serialize)]
struct GroupStateVariables {
    #[serde(rename = "groupKey")]
    group_key: String,
    action: GroupStateActionWire,
    cursor: Option<String>,
    limit: i32,
    #[serde(rename = "idempotencyKey")]
    idempotency_key: String,
}

#[derive(Clone, Copy, Debug, Serialize)]
#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
enum GroupStateActionWire {
    MarkRead,
    MarkUnread,
    Archive,
}

#[derive(Clone, Copy, Debug, Serialize)]
#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
enum GroupItemStateWire {''',
          )
          replace_once(
              adapter,
              '''struct OpenAuthorizationResponse {
    #[serde(rename = "notificationInboxAuthorizeOpen")]
    authorization: OpenAuthorizationWire,
}

#[derive(Debug, Deserialize)]
struct GroupSummaryPageWire {''',
              '''struct OpenAuthorizationResponse {
    #[serde(rename = "notificationInboxAuthorizeOpen")]
    authorization: OpenAuthorizationWire,
}

#[derive(Debug, Deserialize)]
struct GroupStateResponse {
    #[serde(rename = "notificationInboxApplyGroupState")]
    page: GroupStatePageWire,
}

#[derive(Debug, Deserialize)]
struct GroupStatePageWire {
    scanned: u16,
    changed: u16,
    #[serde(rename = "nextCursor")]
    next_cursor: Option<String>,
    #[serde(rename = "hasMore")]
    has_more: bool,
}

#[derive(Debug, Deserialize)]
struct GroupSummaryPageWire {''',
          )
          replace_once(
              adapter,
              '''pub async fn authorize_open(
    access_token: Option<String>,
    tenant_slug: Option<String>,
    request: NotificationStorefrontOpenRequest,
) -> Result<NotificationStorefrontOpenDecision, GraphqlNotificationStorefrontError> {
    let response: OpenAuthorizationResponse = execute_graphql(
        &graphql_url(),
        GraphqlRequest::new(
            OPEN_AUTHORIZATION_QUERY,
            Some(OpenAuthorizationVariables {
                notification_id: request.notification_id,
            }),
        ),
        access_token,
        tenant_slug,
        None,
    )
    .await
    .map_err(|error| error.to_string())?;

    match response.authorization.decision {
        OpenDecisionWire::Allowed => response
            .authorization
            .route
            .map(|route| NotificationStorefrontOpenDecision::Allowed { route })
            .ok_or_else(|| "notification inbox open response is invalid".to_string()),
        OpenDecisionWire::Unavailable => Ok(NotificationStorefrontOpenDecision::Unavailable),
    }
}

fn map_item''',
              '''pub async fn authorize_open(
    access_token: Option<String>,
    tenant_slug: Option<String>,
    request: NotificationStorefrontOpenRequest,
) -> Result<NotificationStorefrontOpenDecision, GraphqlNotificationStorefrontError> {
    let response: OpenAuthorizationResponse = execute_graphql(
        &graphql_url(),
        GraphqlRequest::new(
            OPEN_AUTHORIZATION_QUERY,
            Some(OpenAuthorizationVariables {
                notification_id: request.notification_id,
            }),
        ),
        access_token,
        tenant_slug,
        None,
    )
    .await
    .map_err(|error| error.to_string())?;

    match response.authorization.decision {
        OpenDecisionWire::Allowed => response
            .authorization
            .route
            .map(|route| NotificationStorefrontOpenDecision::Allowed { route })
            .ok_or_else(|| "notification inbox open response is invalid".to_string()),
        OpenDecisionWire::Unavailable => Ok(NotificationStorefrontOpenDecision::Unavailable),
    }
}

pub async fn apply_group_state(
    access_token: Option<String>,
    tenant_slug: Option<String>,
    command: NotificationStorefrontGroupStateCommand,
) -> Result<NotificationStorefrontGroupStatePage, GraphqlNotificationStorefrontError> {
    let response: GroupStateResponse = execute_graphql(
        &graphql_url(),
        GraphqlRequest::new(
            GROUP_STATE_MUTATION,
            Some(GroupStateVariables {
                group_key: command.group_key,
                action: map_group_action_to_wire(command.action),
                cursor: command.cursor,
                limit: i32::from(command.limit),
                idempotency_key: command.idempotency_key,
            }),
        ),
        access_token,
        tenant_slug,
        None,
    )
    .await
    .map_err(|error| error.to_string())?;

    Ok(NotificationStorefrontGroupStatePage {
        scanned: response.page.scanned,
        changed: response.page.changed,
        next_cursor: response.page.next_cursor,
        has_more: response.page.has_more,
    })
}

fn map_item''',
          )
          replace_once(
              adapter,
              '''fn map_state_to_wire(state: NotificationStorefrontItemState) -> GroupItemStateWire {''',
              '''fn map_group_action_to_wire(
    action: NotificationStorefrontGroupStateAction,
) -> GroupStateActionWire {
    match action {
        NotificationStorefrontGroupStateAction::MarkRead => GroupStateActionWire::MarkRead,
        NotificationStorefrontGroupStateAction::MarkUnread => GroupStateActionWire::MarkUnread,
        NotificationStorefrontGroupStateAction::Archive => GroupStateActionWire::Archive,
    }
}

fn map_state_to_wire(state: NotificationStorefrontItemState) -> GroupItemStateWire {''',
          )
          replace_once(
              adapter,
              '''        GROUP_ITEMS_QUERY, GROUP_SUMMARIES_QUERY, OPEN_AUTHORIZATION_QUERY, UNREAD_COUNT_QUERY,
    };''',
              '''        GROUP_ITEMS_QUERY, GROUP_STATE_MUTATION, GROUP_SUMMARIES_QUERY,
        OPEN_AUTHORIZATION_QUERY, UNREAD_COUNT_QUERY,
    };''',
          )
          replace_once(
              adapter,
              '''            GROUP_ITEMS_QUERY,
            OPEN_AUTHORIZATION_QUERY,
        ] {''',
              '''            GROUP_ITEMS_QUERY,
            OPEN_AUTHORIZATION_QUERY,
            GROUP_STATE_MUTATION,
        ] {''',
          )
          replace_once(
              adapter,
              '''    fn open_query_accepts_only_notification_identity_and_returns_typed_decision() {
        assert!(OPEN_AUTHORIZATION_QUERY.contains("$notificationId: String!"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("notificationInboxAuthorizeOpen"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("decision"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("route"));
    }
}''',
              '''    fn open_query_accepts_only_notification_identity_and_returns_typed_decision() {
        assert!(OPEN_AUTHORIZATION_QUERY.contains("$notificationId: String!"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("notificationInboxAuthorizeOpen"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("decision"));
        assert!(OPEN_AUTHORIZATION_QUERY.contains("route"));
    }

    #[test]
    fn group_state_mutation_requires_typed_action_and_idempotency() {
        assert!(GROUP_STATE_MUTATION.contains("mutation NotificationStorefrontApplyGroupState"));
        assert!(GROUP_STATE_MUTATION.contains("$groupKey: String!"));
        assert!(GROUP_STATE_MUTATION.contains("$action: NotificationInboxGroupStateAction!"));
        assert!(GROUP_STATE_MUTATION.contains("$idempotencyKey: String!"));
        assert!(GROUP_STATE_MUTATION.contains("notificationInboxApplyGroupState"));
        assert!(GROUP_STATE_MUTATION.contains("scanned"));
        assert!(GROUP_STATE_MUTATION.contains("changed"));
    }
}''',
          )

          transport = 'crates/rustok-notifications/storefront/src/transport.rs'
          replace_once(
              transport,
              '''pub use native_server_adapter::{
    NativeNotificationStorefrontError, apply_notification_group_state,
    authorize_notification_open as authorize_notification_open_native,''',
              '''pub use native_server_adapter::{
    NativeNotificationStorefrontError,
    apply_notification_group_state as apply_notification_group_state_native,
    authorize_notification_open as authorize_notification_open_native,''',
          )
          replace_once(
              transport,
              '''    NotificationStorefrontGroupSummaryPage, NotificationStorefrontGroupSummaryRequest,
    NotificationStorefrontOpenDecision, NotificationStorefrontOpenRequest,
    NotificationStorefrontState, NotificationStorefrontUnreadCount,
};''',
              '''    NotificationStorefrontGroupStateCommand, NotificationStorefrontGroupStatePage,
    NotificationStorefrontGroupSummaryPage, NotificationStorefrontGroupSummaryRequest,
    NotificationStorefrontOpenDecision, NotificationStorefrontOpenRequest,
    NotificationStorefrontState, NotificationStorefrontUnreadCount,
};''',
          )
          replace_once(
              transport,
              '''fn current_storefront_transport_context() -> NotificationStorefrontTransportContext {''',
              '''fn selected_storefront_write_transport_path() -> UiTransportPath {
    #[cfg(any(feature = "ssr", feature = "hydrate"))]
    {
        UiTransportPath::NativeServer
    }
    #[cfg(not(any(feature = "ssr", feature = "hydrate")))]
    {
        UiTransportPath::Graphql
    }
}

fn current_storefront_transport_context() -> NotificationStorefrontTransportContext {''',
          )
          replace_once(
              transport,
              '''pub async fn load_notification_unread_count(
) -> Result<NotificationStorefrontUnreadCount, NativeNotificationStorefrontError> {''',
              '''pub async fn apply_notification_group_state_selected(
    context: NotificationStorefrontTransportContext,
    command: NotificationStorefrontGroupStateCommand,
) -> UiTransportResult<NotificationStorefrontGroupStatePage> {
    let native_command = command.clone();
    let access_token = context.access_token;
    let tenant_slug = context.tenant_slug;
    execute_selected_transport(
        "notifications.storefront.group_state",
        selected_storefront_write_transport_path(),
        move || apply_notification_group_state_native(native_command),
        move || graphql_adapter::apply_group_state(access_token, tenant_slug, command),
    )
    .await
}

pub async fn load_notification_unread_count(
) -> Result<NotificationStorefrontUnreadCount, NativeNotificationStorefrontError> {''',
          )
          replace_once(
              transport,
              '''pub async fn authorize_notification_open(
    request: NotificationStorefrontOpenRequest,
) -> Result<NotificationStorefrontOpenDecision, NativeNotificationStorefrontError> {
    authorize_notification_open_selected(current_storefront_transport_context(), request)
        .await
        .map_err(|error| NativeNotificationStorefrontError(error.to_string()))
}

pub async fn load_notification_navigation_unread_count''',
              '''pub async fn authorize_notification_open(
    request: NotificationStorefrontOpenRequest,
) -> Result<NotificationStorefrontOpenDecision, NativeNotificationStorefrontError> {
    authorize_notification_open_selected(current_storefront_transport_context(), request)
        .await
        .map_err(|error| NativeNotificationStorefrontError(error.to_string()))
}

pub async fn apply_notification_group_state(
    command: NotificationStorefrontGroupStateCommand,
) -> Result<NotificationStorefrontGroupStatePage, NativeNotificationStorefrontError> {
    apply_notification_group_state_selected(current_storefront_transport_context(), command)
        .await
        .map_err(|error| NativeNotificationStorefrontError(error.to_string()))
}

pub async fn load_notification_navigation_unread_count''',
          )
          replace_once(
              transport,
              '''        NotificationStorefrontTransportContext, selected_storefront_read_transport_path,
    };''',
              '''        NotificationStorefrontTransportContext, selected_storefront_read_transport_path,
        selected_storefront_write_transport_path,
    };''',
          )
          replace_once(
              transport,
              '''    fn integrated_package_profile_selects_native_storefront_reads() {
        assert_eq!(
            selected_storefront_read_transport_path(),
            UiTransportPath::NativeServer
        );
    }

    #[test]
    fn storefront_context_carries_only_transport_credentials() {''',
              '''    fn integrated_package_profile_selects_native_storefront_reads() {
        assert_eq!(
            selected_storefront_read_transport_path(),
            UiTransportPath::NativeServer
        );
    }

    #[cfg(not(any(feature = "ssr", feature = "hydrate")))]
    #[test]
    fn default_package_profile_selects_graphql_for_storefront_writes() {
        assert_eq!(
            selected_storefront_write_transport_path(),
            UiTransportPath::Graphql
        );
    }

    #[cfg(any(feature = "ssr", feature = "hydrate"))]
    #[test]
    fn integrated_package_profile_selects_native_storefront_writes() {
        assert_eq!(
            selected_storefront_write_transport_path(),
            UiTransportPath::NativeServer
        );
    }

    #[test]
    fn storefront_context_carries_only_transport_credentials() {''',
          )

          replace_once(
              'crates/rustok-notifications/storefront/tests/grouped_graphql_contract.rs',
              '''    assert!(STOREFRONT_TRANSPORT.contains("apply_notification_group_state,"));''',
              '''    assert!(STOREFRONT_TRANSPORT.contains("apply_notification_group_state_selected"));
    assert!(STOREFRONT_TRANSPORT.contains("apply_notification_group_state_native"));''',
          )

          replace_once(
              'scripts/verify/verify-forum-notification-inbox-open-graphql.mjs',
              '''requireText(transport, "apply_notification_group_state,", "native group-state command must remain exported");
rejectText(transport, "apply_notification_group_state_selected", "group-state GraphQL parity must remain pending");''',
              '''requireText(transport, "apply_notification_group_state_native", "native group-state command must remain exported");
requireText(transport, "apply_notification_group_state_selected", "downstream group-state transport parity must remain selected without fallback");''',
          )
          replace_once(
              'scripts/verify/verify-forum-notification-inbox-open-graphql.mjs',
              '''  "group-state commands remain native-only",''',
              '''  "notificationInboxApplyGroupState",''',
          )

          storefront_readme = 'crates/rustok-notifications/storefront/README.md'
          replace_once(
              storefront_readme,
              '''The unread count, grouped summaries, and exact-group item pages use one selected read
transport facade. Fresh open authorization now joins the same compile-profile selection
policy:''',
              '''The unread count, grouped summaries, exact-group item pages, fresh open authorization,
and grouped state commands use compile-profile-selected transport facades:''',
          )
          replace_once(
              storefront_readme,
              '''- raw native functions remain available with explicit `_native` names;
- neither transport request accepts tenant, recipient, or user identity;''',
              '''- raw native read, open, and group-state functions remain available with explicit
  `_native` names;
- grouped writes carry one caller-supplied bounded idempotency key through either path;
- neither transport request accepts tenant, recipient, or user identity;''',
          )
          replace_once(
              storefront_readme,
              '''The owner GraphQL schema exposes `notificationInboxUnreadCount`,
`notificationInboxGroupSummaries`, `notificationInboxGroupItems`, and
`notificationInboxAuthorizeOpen`. Manifest-generated schema composition invokes''',
              '''The owner GraphQL schema exposes `notificationInboxUnreadCount`,
`notificationInboxGroupSummaries`, `notificationInboxGroupItems`,
`notificationInboxAuthorizeOpen`, and the mutation
`notificationInboxApplyGroupState`. Manifest-generated schema composition invokes''',
          )
          replace_once(
              storefront_readme,
              '''The navigation unread-count read and grouped inbox reads have native/GraphQL profile
parity. Fresh notification open authorization and group-state commands use separate
security paths: open authorization is now dual-path, while group-state commands remain native-only because they require write admission and an idempotency key. GraphQL command
parity remains a separate gate.''',
              '''The navigation unread-count read and grouped inbox reads have native/GraphQL profile
parity. Fresh notification open authorization and group-state commands now share the same
compile-profile selection rule without fallback. The GraphQL mutation requires a typed action,
one bounded group key, bounded cursor/limit inputs, and a caller-supplied idempotency key;
the owner port still enforces write deadline and idempotency admission before database access.''',
          )
          replace_once(
              storefront_readme,
              '''read/open functions, raw native functions, native group-state commands, and the
serializable storefront request/page models exported from the crate root.''',
              '''read/open/group-state functions, raw native functions, and the serializable storefront
request/page models exported from the crate root.''',
          )

          canonical = 'crates/rustok-forum/docs/implementation-plan.md'
          replace_once(
              canonical,
              '''| `FORUM-20` | `in_progress` | FORUM-20A-AL provide inherited and richer category/topic visibility, recipient-aware Forum notification authorization, the Notifications inbox/group owner plane, authenticated storefront ports, native and GraphQL reads/open authorization, grouped UI and navigation. FORUM-20AM synchronizes the canonical and owner-local ledgers. Write audiences, remaining trust/channel facts, search/index/SEO/deep-link migration, scheduled reconciliation/redaction, delivery transports and PostgreSQL cross-consumer evidence remain. |''',
              '''| `FORUM-20` | `in_progress` | FORUM-20A-AN provide inherited and richer category/topic visibility, recipient-aware Forum notification authorization, the Notifications inbox/group owner plane, authenticated storefront ports, native and GraphQL read/open/write transport parity, grouped UI and navigation. FORUM-20AM synchronizes the ledgers; FORUM-20AN adds GraphQL group-state commands. Write audiences, remaining trust/channel facts, search/index/SEO/deep-link migration, scheduled reconciliation/redaction, delivery transports and PostgreSQL cross-consumer evidence remain. |''',
          )
          replace_once(
              canonical,
              '''- update the latest `FORUM-20AL` handoff contract to point at this synchronization task.

### Compatibility and degraded mode''',
              '''- update the latest `FORUM-20AL` handoff contract to point at this synchronization task.

### Delivered in `FORUM-20AN`

- add the module-owned GraphQL mutation `notificationInboxApplyGroupState` for bounded
  exact-group `MARK_READ`, `MARK_UNREAD`, and `ARCHIVE` commands;
- derive tenant and recipient identity from the authenticated human-user context, require
  module admission before command validation, and carry a five-second deadline plus one
  bounded caller idempotency key through `NotificationInboxStorefrontPort`;
- return only `scanned`, `changed`, `next_cursor`, and `has_more`, preserving owner state,
  timestamp, terminal-archive, pagination, and non-oracular invariants;
- select native writes for SSR/hydrate and GraphQL writes for CSR/headless without fallback,
  while keeping the existing UI call site and authoritative post-command refresh behavior.

### Compatibility and degraded mode''',
          )
          replace_once(
              canonical,
              '''shadow inbox data. Native and GraphQL storefront reads select exactly one transport path
with no cross-path fallback. Group-state writes remain native-only until GraphQL write
admission and idempotency parity are implemented.''',
              '''shadow inbox data. Native and GraphQL storefront reads and writes select exactly one
compile-profile transport path with no cross-path fallback. Both group-state paths delegate
to the same owner port; the GraphQL path cannot select another tenant or recipient and must
supply write deadline and idempotency semantics.''',
          )
          replace_once(
              canonical,
              '''- add auth-reactive automatic grouped-inbox bootstrap refresh and GraphQL group-state
  mutations with deadline and idempotency admission;''',
              '''- add auth-reactive automatic grouped-inbox bootstrap refresh without requiring an
  explicit resource refresh;''',
          )
          replace_once(
              canonical,
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
          )

          local_plan = 'crates/rustok-notifications/docs/implementation-plan.md'
          replace_once(
              local_plan,
              '''SSR/hydrate, GraphQL serves CSR/headless grouped reads and fresh open authorization, and no
transport fallback is permitted.''',
              '''SSR/hydrate, while GraphQL serves CSR/headless grouped reads, fresh open authorization,
and bounded group-state writes; no transport fallback is permitted.''',
          )
          replace_once(
              local_plan,
              '''Notifications route and exact unread badge. GraphQL group-state writes, automatic auth-change
bootstrap refresh, tenant-wide scheduling/redaction, delivery transports, and PostgreSQL
cross-consumer evidence remain open.''',
              '''Notifications route and exact unread badge. Automatic auth-change bootstrap refresh,
tenant-wide scheduling/redaction, delivery transports, and PostgreSQL cross-consumer evidence
remain open.''',
          )
          replace_once(
              local_plan,
              '''### `FORUM-20AM`

- canonical Forum plan, this owner-local ledger, owner README, and live contract are
  synchronized through `FORUM-20AL`;
- the latest handoff contract records the synchronization and a dedicated static verifier
  guards all four documents from future milestone drift;
- no runtime capability or validation status is promoted.

## Remaining `NOTIFY-01`''',
              '''### `FORUM-20AM`

- canonical Forum plan, this owner-local ledger, owner README, and live contract are
  synchronized through `FORUM-20AL`;
- the latest handoff contract records the synchronization and a dedicated static verifier
  guards all four documents from future milestone drift;
- no runtime capability or validation status is promoted.

### `FORUM-20AN`

- GraphQL mutation parity for bounded exact-group mark-read, mark-unread, and archive;
- typed action plus bounded group/cursor/limit inputs and a required bounded idempotency key;
- authenticated context-derived tenant/recipient scope with five-second write deadline;
- selected native SSR/hydrate and GraphQL CSR/headless command paths without fallback;
- unchanged owner state service, timestamp invariants, terminal archive, and UI refresh flow.

## Remaining `NOTIFY-01`''',
          )
          replace_once(
              local_plan,
              '''shadow inbox storage. Group-state writes remain on the native path until GraphQL write
admission and idempotency parity are delivered.''',
              '''shadow inbox storage. Group-state writes select the native SSR/hydrate path or the
GraphQL CSR/headless path without fallback, and both preserve owner write admission plus
caller idempotency semantics.''',
          )
          replace_once(
              local_plan,
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
          )
          replace_once(
              local_plan,
              '''`FORUM-20R/20S/20T/20U/20V/20W/20X/20Y/20Z/20AA/20AB/20AC/20AD/20AE/20AF/20AG/20AH/20AI/20AJ/20AK/20AL/20AM` source and documentation slices.''',
              '''`FORUM-20R/20S/20T/20U/20V/20W/20X/20Y/20Z/20AA/20AB/20AC/20AD/20AE/20AF/20AG/20AH/20AI/20AJ/20AK/20AL/20AM/20AN` source and documentation slices.''',
          )

          owner_readme = 'crates/rustok-notifications/README.md'
          replace_once(
              owner_readme,
              '''there is no transport fallback and no direct storefront database authority. GraphQL state
mutations remain intentionally absent.''',
              '''there is no transport fallback and no direct storefront database authority. GraphQL now
exposes the same bounded exact-group state commands with typed actions and explicit
idempotency admission.''',
          )
          replace_once(
              owner_readme,
              '''- GraphQL group-state mutations and auth-reactive automatic grouped bootstrap refresh;''',
              '''- auth-reactive automatic grouped bootstrap refresh;''',
          )

          live_readme = 'crates/rustok-notifications/docs/README.md'
          replace_once(
              live_readme,
              '''other and GraphQL group-state mutations remain closed.''',
              '''other. GraphQL group-state mutations now delegate to the same owner port and require
explicit idempotency plus deadline admission.''',
          )
          replace_once(
              live_readme,
              '''- GraphQL group-state mutations and auth-reactive automatic grouped bootstrap refresh;''',
              '''- auth-reactive automatic grouped bootstrap refresh;''',
          )
          replace_once(
              live_readme,
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
              '''node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs
node scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs
node scripts/verify/verify-forum-notification-plan-sync.mjs''',
          )
          replace_once(
              live_readme,
              '''`FORUM-20R/20S/20T/20U/20V/20W/20X/20Y/20Z/20AA/20AB/20AC/20AD/20AE/20AF/20AG/20AH/20AI/20AJ/20AK/20AL/20AM`.''',
              '''`FORUM-20R/20S/20T/20U/20V/20W/20X/20Y/20Z/20AA/20AB/20AC/20AD/20AE/20AF/20AG/20AH/20AI/20AJ/20AK/20AL/20AM/20AN`.''',
          )

          contract = {
              'schema_version': 1,
              'task': 'FORUM-20AN',
              'upstream_task': 'FORUM-20AM',
              'residual_task': 'FORUM-20AL',
              'scope': 'GraphQL parity for bounded authenticated notification exact-group state commands',
              'notifications_graphql_file': graphql,
              'notifications_manifest': 'crates/rustok-notifications/rustok-module.toml',
              'notifications_storefront_port': 'crates/rustok-notifications/src/inbox_storefront_port.rs',
              'storefront_graphql_adapter_file': adapter,
              'storefront_transport_file': transport,
              'storefront_ui_file': 'crates/rustok-notifications/storefront/src/ui/leptos.rs',
              'storefront_proof': 'crates/rustok-notifications/storefront/tests/group_state_graphql_contract.rs',
              'owner_note': 'crates/rustok-notifications/docs/forum-20an-group-state-graphql.md',
              'canonical_plan': canonical,
              'notifications_local_plan': local_plan,
              'notifications_owner_readme': owner_readme,
              'notifications_live_contract': live_readme,
              'source_verifier': 'scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs',
              'composition': {
                  'owner_mutation_root': True,
                  'typed_group_action': True,
                  'bounded_progress_page': True,
                  'human_user_required': True,
                  'service_principal_rejected': True,
                  'auth_tenant_match_required': True,
                  'module_enabled_required': True,
                  'admission_before_command_validation': True,
                  'context_derived_tenant_scope': True,
                  'context_derived_recipient_scope': True,
                  'graphql_request_excludes_owner_identity': True,
                  'five_second_write_deadline': True,
                  'bounded_idempotency_key': True,
                  'idempotency_forwarded_to_port_context': True,
                  'storefront_channel_context': True,
                  'permission_claim_forwarding': True,
                  'storefront_port_reused': True,
                  'owner_group_state_service_reused': True,
                  'native_write_path': True,
                  'graphql_write_path': True,
                  'no_transport_fallback': True,
                  'compatibility_write_wrapper': True,
                  'ui_call_site_preserved': True,
                  'authoritative_refresh_preserved': True,
                  'direct_storefront_database_query': False,
                  'parallel_state_service': False,
                  'selected_id_bulk_graphql': False,
                  'local_storage': False,
                  'shadow_inbox': False,
                  'channel_delivery': False,
              },
              'not_delivered': [
                  'auth-reactive automatic grouped inbox bootstrap refresh without explicit resource refresh',
                  'GraphQL selected-ID state commands',
                  'tenant-wide scheduled reconciliation and payload redaction',
                  'channel delivery enqueue and transports',
                  'delivery-time target authorization',
                  'PostgreSQL runtime execution and cross-consumer evidence',
              ],
              'verification': {
                  'commands': [
                      'cargo test -p rustok-notifications --features server --lib graphql -- --nocapture',
                      'cargo test -p rustok-notifications-storefront --test group_state_graphql_contract -- --nocapture',
                      'cargo test -p rustok-notifications-storefront --test grouped_graphql_contract -- --nocapture',
                      'cargo check -p rustok-notifications --features server',
                      'cargo check -p rustok-notifications-storefront --features ssr',
                      'cargo check -p rustok-notifications-storefront',
                      'cargo check -p rustok-server --features mod-notifications',
                      'node scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs',
                      'node scripts/verify/verify-forum-notification-inbox-open-graphql.mjs',
                      'node scripts/verify/verify-forum-notification-inbox-grouped-graphql.mjs',
                      'cargo xtask module validate notifications',
                      'cargo xtask module validate forum',
                  ],
                  'execution_status': 'not_run_by_implementation_agent',
              },
          }
          write(
              'crates/rustok-forum/contracts/forum-notification-inbox-group-state-graphql.json',
              json.dumps(contract, indent=2) + '\n',
          )

          write(
              'crates/rustok-notifications/docs/forum-20an-group-state-graphql.md',
              '''# FORUM-20AN notification group-state GraphQL commands

Status: source-ready / unvalidated

## Scope

`FORUM-20AN` closes the GraphQL group-state command residual left by
`FORUM-20AL`. It exposes bounded exact-group `MARK_READ`, `MARK_UNREAD`, and
`ARCHIVE` through the same authenticated storefront owner port used by the
native Leptos server-function path.

## Admission and owner scope

The mutation `notificationInboxApplyGroupState` requires an authenticated human
user, matching auth/tenant context, and the enabled Notifications capability
before validating the caller idempotency key or invoking the owner port. Tenant
and recipient are derived from request context and cannot be supplied as GraphQL
arguments.

The mutation accepts one opaque group key, one typed action, optional bounded
cursor/limit inputs, and one non-empty control-free idempotency key capped at 128
bytes. The generated `PortContext` carries the canonical user actor, permission
claims, effective locale, storefront channel, correlation identity, a five-second
write deadline, and the caller idempotency key.

## Owner and transport behavior

The resolver delegates only to
`NotificationInboxStorefrontPort::apply_group_state`, which preserves the
existing owner action-specific selection, exact tenant/recipient/group scope,
state timestamp invariants, terminal archive, and bounded continuation contract.
The GraphQL response exposes only scanned/changed counts and continuation
metadata.

SSR and hydrate builds select the native command path. CSR and headless builds
select the GraphQL mutation. No path falls back to the other. The existing UI
call site remains unchanged and still performs an authoritative resource refresh
after every successful command.

## Boundary

This slice adds no selected-ID GraphQL commands, shadow storage, direct storefront
database access, automatic auth-reactive bootstrap refresh, scheduled
reconciliation/redaction, channel delivery, or PostgreSQL runtime evidence.

Suggested maintainer validation commands are recorded in the machine contract.
Tests, Cargo commands, formatting, verifiers, workflows, and CI validation were
not run by the implementation agent.
''',
          )

          write(
              'crates/rustok-notifications/storefront/tests/group_state_graphql_contract.rs',
              '''const OWNER_GRAPHQL: &str = include_str!("../../src/graphql.rs");
const OWNER_MANIFEST: &str = include_str!("../../rustok-module.toml");
const OWNER_PORT: &str = include_str!("../../src/inbox_storefront_port.rs");
const STOREFRONT_GRAPHQL: &str = include_str!("../src/transport/graphql_adapter.rs");
const STOREFRONT_TRANSPORT: &str = include_str!("../src/transport.rs");
const STOREFRONT_UI: &str = include_str!("../src/ui/leptos.rs");

#[test]
fn owner_mutation_admits_before_bounded_command_and_reuses_port() {
    for marker in [
        "pub struct NotificationsMutation",
        "async fn notification_inbox_apply_group_state",
        "let scope = authenticated_scope(ctx)?;",
        "require_module_enabled(ctx, MODULE_SLUG).await?",
        "parse_idempotency_key(idempotency_key)?",
        "scope.write_port_context(\"group-state\", idempotency_key)",
        "NotificationInboxStorefrontGroupStateRequest",
        ".apply_group_state(",
        "GqlNotificationInboxGroupStateAction",
        "GqlNotificationInboxGroupStatePage",
        "GRAPHQL_WRITE_DEADLINE",
        "MAX_IDEMPOTENCY_KEY_BYTES",
        ".with_idempotency_key(idempotency_key)",
        ".with_channel(\"storefront\")",
    ] {
        assert!(OWNER_GRAPHQL.contains(marker), "owner GraphQL is missing `{marker}`");
    }
    assert!(OWNER_MANIFEST.contains("mutation = \"graphql::NotificationsMutation\""));
    assert!(OWNER_PORT.contains("PortCallPolicy::write()"));
    assert!(OWNER_PORT.contains("NotificationInboxGroupStateService"));

    let signature = OWNER_GRAPHQL
        .split("async fn notification_inbox_apply_group_state")
        .nth(1)
        .and_then(|source| source.split(") -> Result<GqlNotificationInboxGroupStatePage>").next())
        .expect("mutation signature should exist");
    for forbidden in ["tenant_id", "recipient_id", "user_id"] {
        assert!(!signature.contains(forbidden));
    }
}

#[test]
fn graphql_wire_carries_typed_action_idempotency_and_progress_only() {
    for marker in [
        "mutation NotificationStorefrontApplyGroupState",
        "$groupKey: String!",
        "$action: NotificationInboxGroupStateAction!",
        "$cursor: String",
        "$limit: Int",
        "$idempotencyKey: String!",
        "notificationInboxApplyGroupState",
        "GroupStateActionWire",
        "pub async fn apply_group_state",
        "scanned",
        "changed",
        "nextCursor",
        "hasMore",
    ] {
        assert!(STOREFRONT_GRAPHQL.contains(marker), "adapter is missing `{marker}`");
    }
    let production = STOREFRONT_GRAPHQL
        .split("#[cfg(test)]")
        .next()
        .expect("production adapter should exist");
    for forbidden in ["tenantId", "recipientId", "userId", "serde_json::Value"] {
        assert!(!production.contains(forbidden));
    }
}

#[test]
fn selected_write_path_preserves_native_and_ui_without_fallback() {
    for marker in [
        "apply_notification_group_state as apply_notification_group_state_native",
        "selected_storefront_write_transport_path",
        "pub async fn apply_notification_group_state_selected",
        "notifications.storefront.group_state",
        "apply_notification_group_state_native(native_command)",
        "graphql_adapter::apply_group_state",
        "pub async fn apply_notification_group_state(",
        "current_storefront_transport_context()",
    ] {
        assert!(STOREFRONT_TRANSPORT.contains(marker), "transport is missing `{marker}`");
    }
    assert!(!STOREFRONT_TRANSPORT.contains("fallback_failed"));
    assert!(STOREFRONT_UI.contains("apply_notification_group_state("));
    assert!(STOREFRONT_UI.contains("on_refresh.run(feedback)"));
}
''',
          )

          verifier = '''#!/usr/bin/env node

import { existsSync, readFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const scriptDir = path.dirname(fileURLToPath(import.meta.url));
const repoRoot = process.env.RUSTOK_VERIFY_REPO_ROOT
  ? path.resolve(process.env.RUSTOK_VERIFY_REPO_ROOT)
  : path.resolve(scriptDir, "../..");
const failures = [];

function read(relativePath) {
  const absolute = path.join(repoRoot, relativePath);
  if (!existsSync(absolute)) {
    failures.push(`${relativePath}: required file is missing`);
    return "";
  }
  return readFileSync(absolute, "utf8");
}
function requireText(source, marker, message) {
  if (!source.includes(marker)) failures.push(message);
}
function rejectText(source, marker, message) {
  if (source.includes(marker)) failures.push(message);
}
function between(source, start, end, label) {
  const from = source.indexOf(start);
  const to = source.indexOf(end, from + start.length);
  if (from < 0 || to < 0 || to <= from) {
    failures.push(`${label}: bounded section is missing`);
    return "";
  }
  return source.slice(from, to);
}

const contract = JSON.parse(read("crates/rustok-forum/contracts/forum-notification-inbox-group-state-graphql.json") || "{}");
const owner = read(contract.notifications_graphql_file ?? "");
const manifest = read(contract.notifications_manifest ?? "");
const port = read(contract.notifications_storefront_port ?? "");
const adapter = read(contract.storefront_graphql_adapter_file ?? "");
const transport = read(contract.storefront_transport_file ?? "");
const ui = read(contract.storefront_ui_file ?? "");
const proof = read(contract.storefront_proof ?? "");
const note = read(contract.owner_note ?? "");
const canonical = read(contract.canonical_plan ?? "");
const local = read(contract.notifications_local_plan ?? "");
const ownerReadme = read(contract.notifications_owner_readme ?? "");
const live = read(contract.notifications_live_contract ?? "");
const residual = JSON.parse(read("crates/rustok-forum/contracts/forum-notification-inbox-open-graphql.json") || "{}");

if (contract.schema_version !== 1 || contract.task !== "FORUM-20AN" || contract.upstream_task !== "FORUM-20AM" || contract.residual_task !== "FORUM-20AL") {
  failures.push("group-state GraphQL contract must connect FORUM-20AL/20AM/20AN");
}
if (contract.verification?.execution_status !== "not_run_by_implementation_agent") {
  failures.push("group-state GraphQL contract must not claim unexecuted evidence");
}
for (const key of [
  "owner_mutation_root", "typed_group_action", "bounded_progress_page",
  "human_user_required", "service_principal_rejected", "auth_tenant_match_required",
  "module_enabled_required", "admission_before_command_validation",
  "context_derived_tenant_scope", "context_derived_recipient_scope",
  "graphql_request_excludes_owner_identity", "five_second_write_deadline",
  "bounded_idempotency_key", "idempotency_forwarded_to_port_context",
  "storefront_channel_context", "permission_claim_forwarding", "storefront_port_reused",
  "owner_group_state_service_reused", "native_write_path", "graphql_write_path",
  "no_transport_fallback", "compatibility_write_wrapper", "ui_call_site_preserved",
  "authoritative_refresh_preserved",
]) {
  if (contract.composition?.[key] !== true) failures.push(`contract must record ${key}`);
}
for (const key of ["direct_storefront_database_query", "parallel_state_service", "selected_id_bulk_graphql", "local_storage", "shadow_inbox", "channel_delivery"]) {
  if (contract.composition?.[key] !== false) failures.push(`contract must keep ${key} false`);
}

for (const marker of [
  "pub struct NotificationsMutation", "async fn notification_inbox_apply_group_state",
  "GqlNotificationInboxGroupStateAction", "GqlNotificationInboxGroupStatePage",
  "let scope = authenticated_scope(ctx)?;", "require_module_enabled(ctx, MODULE_SLUG).await?",
  "parse_idempotency_key(idempotency_key)?", "MAX_IDEMPOTENCY_KEY_BYTES",
  "scope.write_port_context(\"group-state\", idempotency_key)",
  ".with_deadline(deadline)", ".with_idempotency_key(idempotency_key)",
  ".with_channel(\"storefront\")", ".apply_group_state(",
]) requireText(owner, marker, `owner GraphQL is missing ${marker}`);
requireText(manifest, "mutation = \"graphql::NotificationsMutation\"", "manifest must publish mutation root");
requireText(port, "PortCallPolicy::write()", "owner port must enforce write policy");
requireText(port, "NotificationInboxGroupStateService", "owner port must reuse group state service");

const mutation = between(owner, "async fn notification_inbox_apply_group_state", ") -> Result<GqlNotificationInboxGroupStatePage>", "group-state mutation signature");
for (const forbidden of ["tenant_id", "recipient_id", "user_id"]) rejectText(mutation, forbidden, `mutation must not accept ${forbidden}`);
const mutationBody = between(owner, "async fn notification_inbox_apply_group_state", "#[derive(Clone)]", "group-state mutation body");
const authIndex = mutationBody.indexOf("let scope = authenticated_scope(ctx)?;");
const moduleIndex = mutationBody.indexOf("require_module_enabled(ctx, MODULE_SLUG).await?");
const idempotencyIndex = mutationBody.indexOf("parse_idempotency_key(idempotency_key)?");
const portIndex = mutationBody.indexOf(".apply_group_state(");
if (!(authIndex >= 0 && moduleIndex > authIndex && idempotencyIndex > moduleIndex && portIndex > idempotencyIndex)) {
  failures.push("mutation must admit auth/module before command validation and owner access");
}

for (const marker of [
  "mutation NotificationStorefrontApplyGroupState", "$groupKey: String!",
  "$action: NotificationInboxGroupStateAction!", "$idempotencyKey: String!",
  "notificationInboxApplyGroupState", "GroupStateActionWire", "GroupStateVariables",
  "pub async fn apply_group_state", "scanned", "changed", "nextCursor", "hasMore",
]) requireText(adapter, marker, `GraphQL adapter is missing ${marker}`);
const adapterProduction = adapter.split("#[cfg(test)]")[0];
for (const forbidden of ["tenantId", "recipientId", "userId", "serde_json::Value"]) rejectText(adapterProduction, forbidden, `GraphQL adapter must not expose ${forbidden}`);

for (const marker of [
  "apply_notification_group_state as apply_notification_group_state_native",
  "selected_storefront_write_transport_path", "apply_notification_group_state_selected",
  "notifications.storefront.group_state", "apply_notification_group_state_native(native_command)",
  "graphql_adapter::apply_group_state", "pub async fn apply_notification_group_state(",
  "current_storefront_transport_context()",
]) requireText(transport, marker, `selected write transport is missing ${marker}`);
rejectText(transport, "fallback_failed", "group-state transport must not add fallback");
requireText(ui, "apply_notification_group_state(", "UI must keep compatibility command wrapper");
requireText(ui, "on_refresh.run(feedback)", "UI must preserve authoritative refresh");

for (const marker of [
  "owner_mutation_admits_before_bounded_command_and_reuses_port",
  "graphql_wire_carries_typed_action_idempotency_and_progress_only",
  "selected_write_path_preserves_native_and_ui_without_fallback",
]) requireText(proof, marker, `source proof is missing ${marker}`);
for (const marker of ["# FORUM-20AN notification group-state GraphQL commands", "source-ready / unvalidated", "notificationInboxApplyGroupState", "does not claim maintainer-run tests"]) requireText(note, marker, `owner note is missing ${marker}`);
for (const marker of ["FORUM-20A-AN provide", "### Delivered in `FORUM-20AN`", "auth-reactive automatic grouped-inbox bootstrap refresh"]) requireText(canonical, marker, `canonical plan is missing ${marker}`);
for (const marker of ["### `FORUM-20AN`", "GraphQL CSR/headless path without fallback"]) requireText(local, marker, `local plan is missing ${marker}`);
for (const marker of ["notificationInboxApplyGroupState", "typed actions and explicit", "auth-reactive automatic grouped bootstrap refresh"]) requireText(ownerReadme, marker, `owner README is missing ${marker}`);
for (const marker of ["GraphQL group-state mutations now delegate", "auth-reactive automatic grouped bootstrap refresh"]) requireText(live, marker, `live contract is missing ${marker}`);
if (residual.composition?.group_state_graphql_parity !== false || !residual.not_delivered?.includes("GraphQL group-state commands")) failures.push("FORUM-20AN must close the historical FORUM-20AL group-state residual");

if (failures.length > 0) {
  console.error("Forum notification group-state GraphQL verification failed:");
  for (const failure of failures) console.error(`- ${failure}`);
  process.exit(1);
}
console.log("Forum notification group-state GraphQL contract is source-ready.");
'''
          write('scripts/verify/verify-forum-notification-inbox-group-state-graphql.mjs', verifier)

          # Ensure the current README markers used by the historical grouped/open verifiers survive downstream closure.
          if 'Fresh notification open authorization and group-state commands now share' not in read(storefront_readme):
              raise SystemExit('storefront README downstream marker missing')

          Path('.github/workflows/agent-forum-group-state-graphql-20an.yml').unlink()
          PY

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(notifications): add GraphQL group-state commands"
          git push origin HEAD
