name: Payment execution hardening bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch is applied by the trusted main runner"
