# Archived Loco Feature Inventory

Status: archived. The active server runtime is pure Axum and no active Cargo
manifest depends on `loco-rs`.

This filename is retained only for stable historical links. Do not use this
document as architecture guidance or as a source for implementation patterns.
The replacement is the [Loco RS Exit Plan](../../../docs/architecture/loco-exit-plan.md),
with current runtime details in the [server documentation](README.md).

The historical upstream snapshot remains audit material only and is not part of
the executable server contract.
