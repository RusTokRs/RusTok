# Event contract digest admission

Status: `baseline_admitted_verified_product_index_family_digest_pending`.

## Purpose

`crates/rustok-events/contracts/event-contract-digests.json` is a reviewed release artifact, not a value that
should be reconstructed by hand. The registry digest is derived from the complete sorted event schema registry,
while the root and typed payload/envelope digests depend on the exact Schemars output produced by the repository
generator and crate dependencies.

The previously stale baseline was regenerated by the maintainer, reviewed and admitted through PR #3390. The
admitted commit is `7983092f96e14c002c57451709de936e40c01356`. The maintainer then ran the source guard and canonical
generator again on that exact commit; both completed successfully and the digest diff was empty.

That exact-SHA local execution is the maintainer-provided verification result for the pre-release repository
boundary. No GitHub Actions verification packet is claimed or fabricated. The repository-owned generator remains
the sole hashing authority.

The baseline gate that blocked `ProductIndexRefreshEvent` is therefore complete. The family may now be added, but
adding it changes the typed registry and contract schemas, so its generated digest must be regenerated and
committed in the same reviewed wire-contract PR before merge.

## Workflow

The **Event contract digest admission** GitHub Actions workflow remains the reusable read-only packet path.

It supports two modes:

- `generate_patch`: run the canonical generator, retain the generated artifact and patch, and complete successfully
  when drift exists;
- `verify`: run the same generator and fail when the committed artifact differs from canonical output.

The generator command is exactly:

```bash
cargo run --locked -p rustok-events --example event_contract_digests -- --write
```

No alternative hashing implementation, copied digest, hand-authored JSON Schema or parallel generator is admitted.

## Review packet

When the workflow is used, it archives:

- `event-contract-digests.committed.json` — artifact before generation;
- `event-contract-digests.generated.json` — canonical generator output;
- `event-contract-digests.patch` — exact repository diff;
- `manifest.env` — source SHA/ref, toolchain, mode, status and generator command;
- `SHA256SUMS` — hashes for packet files.

The packet is diagnostic and reviewable. It does not automatically write the repository. A generated artifact
must still be intentionally admitted through normal review.

## Write safety

The workflow has only `contents: read` permission and checks out with `persist-credentials: false`. It does not
commit, push, open a pull request or run from scheduled/push/pull-request triggers.

The same write-safety rule applies to local generation: inspect the exact diff and admit only the generated JSON;
do not hand-edit digest values.

## Product Index dependency

The stale-baseline gate is complete:

1. canonical drift was generated on reviewed `main`;
2. the generated/committed JSON pair and exact patch were inspected;
3. the canonical generated artifact was admitted through PR #3390;
4. the maintainer reran the repository source guard and canonical generator on admitted
   `7983092f96e14c002c57451709de936e40c01356`, with an empty digest diff.

The now-open source step is:

5. add `ProductIndexRefreshEvent` and regenerate `event-contract-digests.json` again in that same reviewed
   wire-contract PR;
6. require the generated Product-family digest artifact to be committed before merge.

The existing Product locale/ProductVariant ledgers, canonical writer and durable relay step remain authoritative
for identity/causation; the typed family does not bypass them.

## Current Product-family boundary

The Product family source must remain narrow:

- locale target: `product_id`, `locale`, `source_version`;
- variant target: `product_id`, `variant_id`, `source_version`;
- envelope identity/correlation: Product ledger `refresh_id`;
- envelope causation: Product ledger `root_event_id`;
- tenant and actor remain envelope metadata.

No legacy/v2 payload, compatibility decoder or dual publication family is permitted for this repository-owned
pre-release contract.

## Maintainer commands

For the Product-family PR, run on its exact head:

```bash
node scripts/verify/verify-event-contract-digest-admission.mjs
cargo run --locked -p rustok-events --example event_contract_digests -- --write
git diff -- crates/rustok-events/contracts/event-contract-digests.json
```

After admitting that generated diff in the same PR:

```bash
node scripts/verify/verify-index-product-refresh-event-family.mjs
```

The implementation agent does not claim Product-family digest generation or the new family verifier has executed
on the in-review branch yet.
