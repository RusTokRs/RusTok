{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rustok.dev/schemas/index-storage-decision-v2.json",
  "title": "RusToK Index PostgreSQL storage decision",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status",
    "decision_date",
    "owner",
    "comparison_commit",
    "comparison_sha256",
    "selected_prototype",
    "selection_rationale",
    "rejection_rationales",
    "operational_tradeoffs",
    "migration_strategy",
    "rollback_strategy"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "const": "./storage-decision.schema.json"
    },
    "status": {
      "type": "string",
      "enum": ["proposed", "accepted"]
    },
    "decision_date": {
      "type": "string",
      "format": "date"
    },
    "owner": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "comparison_commit": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{40}$"
    },
    "comparison_sha256": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{64}$"
    },
    "selected_prototype": {
      "type": "string",
      "enum": ["jsonb", "typed_eav", "hot_projection"]
    },
    "selection_rationale": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "rejection_rationales": {
      "type": "object"
    },
    "operational_tradeoffs": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "migration_strategy": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "rollback_strategy": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "selected_prototype": { "const": "jsonb" }
        },
        "required": ["selected_prototype"]
      },
      "then": {
        "properties": {
          "rejection_rationales": {
            "type": "object",
            "additionalProperties": false,
            "required": ["typed_eav", "hot_projection"],
            "properties": {
              "typed_eav": { "type": "string", "minLength": 1, "pattern": "\\S" },
              "hot_projection": { "type": "string", "minLength": 1, "pattern": "\\S" }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "selected_prototype": { "const": "typed_eav" }
        },
        "required": ["selected_prototype"]
      },
      "then": {
        "properties": {
          "rejection_rationales": {
            "type": "object",
            "additionalProperties": false,
            "required": ["jsonb", "hot_projection"],
            "properties": {
              "jsonb": { "type": "string", "minLength": 1, "pattern": "\\S" },
              "hot_projection": { "type": "string", "minLength": 1, "pattern": "\\S" }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "selected_prototype": { "const": "hot_projection" }
        },
        "required": ["selected_prototype"]
      },
      "then": {
        "properties": {
          "rejection_rationales": {
            "type": "object",
            "additionalProperties": false,
            "required": ["jsonb", "typed_eav"],
            "properties": {
              "jsonb": { "type": "string", "minLength": 1, "pattern": "\\S" },
              "typed_eav": { "type": "string", "minLength": 1, "pattern": "\\S" }
            }
          }
        }
      }
    }
  ]
}
